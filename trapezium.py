# Calculate area of a trapezium (trapezoid)
# Ask for parallel sides a and b, and height h
a = float(input("Enter first parallel side (a): "))
b = float(input("Enter second parallel side (b): "))
h = float(input("Enter height (h): "))
area = 0.5 * (a + b) * h
print("Area of trapezium:", area)
